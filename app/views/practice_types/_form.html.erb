<%= form_for(@practice_type) do |f| %>
  <% if @practice_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@practice_type.errors.count, "error") %> prohibited this practice_type from being saved:</h2>

      <ul>
      <% @practice_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
