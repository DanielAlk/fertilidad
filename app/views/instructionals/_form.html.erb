<%= form_for(@instructional) do |f| %>
  <% if @instructional.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instructional.errors.count, "error") %> prohibited this instructional from being saved:</h2>

      <ul>
      <% @instructional.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :subtitle %><br>
    <%= f.text_field :subtitle %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text, rows: 15, class: 'tinymce' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%= tinymce_init %>